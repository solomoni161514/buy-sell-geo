{
  "name": "buy-sell-geo-server",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "cd ../client && npm ci && npm run build || true",
    "dev": "nodemon index.js",
    "postinstall": "npm --prefix server ci"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^7.5.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}